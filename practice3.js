
//Questions -- A "good string" is a string that starts with a & has a lenght > 3.
let str = "a good time waits";

if((str[0] == 'a') && (str.length > 3)){
        console.log("Good String");
}else{
        console.log("Bad String");
}